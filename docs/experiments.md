# 实验记录

口径见 `src/msa/metrics.py`：`non0` = 剔除中性样本的二分类，`has0` = 非负/负全样本二分类。

- **所有 ± 号后是样本标准差（ddof=1）**，即"再跑几个 seed 会有多大波动"的无偏估计。
- 每条记录对应 `outputs/<group>/seed<N>/result.json`（超参、环境指纹、git commit、逐 epoch 历史、预测哈希），同级 `summary.json` 为多 seed 汇总。
- `python scripts/verify_runs.py` 可从落盘预测重算全部指标并校验汇总，**下表每个数字都通过该检查**。

## CMU-MOSI，LF-LSTM 基线

aligned_50，bs 32，lr 1e-3，dropout 0.2，按 valid MAE 选模型，CUDA：

| seed | MAE ↓ | Corr ↑ | Acc-2(non0) | Acc-2(has0) | Acc-7 |
|------|-------|--------|-------------|-------------|-------|
| 42-46（5 seed） | 0.9708 ± 0.0387 | 0.6460 ± 0.0110 | 0.7637 ± 0.0124 | 0.7525 ± 0.0101 | 0.3464 ± 0.0262 |
| 仅 42（CPU，8 线程） | 0.9800 | 0.6398 | 0.7713 | 0.7609 | 0.3469 |

**单 seed 的 MAE 在 [0.9420, 1.0375] 之间波动，标准差 0.039。这比很多论文声称的模型改进还大；任何小于该幅度的差距都不构成结论。**

CUDA seed 42 的预测哈希 `172967c7dced83b2`，CPU seed 42 为 `0201e2133a68f4e9`——同代码同 seed，仅设备不同，原因见 README「复现性」一节。

## 复现结果总表（10 seed，42-51，忠实移植）

**解读见 [`storyline.md`](storyline.md)**，此处只列数字。判定规则：落后 MMSA 报告值 ≤1 SE 通过，1-2 SE 通过但标记，>2 SE 未复现。

| 模型 | 数据 | MAE ↓ | Acc-2(non0) | Acc-7 | Corr | 判定 |
|---|---|---|---|---|---|---|
| TFN | unaligned | 0.9511 ± 0.0249 | 0.7855 ± 0.0165 | 0.3528 ± 0.0187 | 0.6602 ± 0.0095 | **复现成功** |
| *MMSA 报告* | | *0.9473* | *0.7908* | *0.3446* | *0.6733* | |
| LMF | unaligned | 0.9513 ± 0.0335 | 0.7854 ± 0.0128 | 0.3362 ± 0.0172 | 0.6528 ± 0.0121 | **复现成功** |
| *MMSA 报告* | | *0.9504* | *0.7918* | *0.3382* | *0.6510* | |

参数量：TFN 9.50M / LMF 0.505M（19 倍压缩，指标持平）。

### 消融：我们的掩码池化 vs MMSA 的 padding 行为

| 模型 | 忠实移植 MAE | 掩码版 MAE | 忠实 Acc-2 | 掩码 Acc-2 |
|---|---|---|---|---|
| TFN | 0.9511 | 0.9546 | 0.7855 | 0.7779 |
| LMF | 0.9513 | 0.9628 | 0.7854 | 0.7803 |

**同一个改动在两个模型上方向不同**：TFN 上掩码版 MAE 略好，LMF 上明显更差（+2.4 SE）。两者的 Acc-2 都因掩码而下降。

### 其它诊断（均 10 seed）

| 配置 | MAE | 说明 |
|---|---|---|
| LMF 复刻 MMSA 的参数冻结 quirk | 0.9806 | 比全参数训练差 0.018，确认是 bug |
| LMF 施加 MMSA 声明但未调用的 dropout | 0.9906 | 比不施加差 0.028 |
| TFN 裁剪梯度 1.0（我们旧默认） | 0.9585 | MMSA 不裁剪 |

## padding 掩码消融

背景：aligned_50 有 70.4% 的时间步是 padding（文本平均 14.8 token / 50 步），且 **BERT 给 `[PAD]` 算出的嵌入不是零**（1275/1284 个训练样本如此）。取 `h[-1]` 等于让 LSTM 又跑了约 35 步 `[PAD]` 才读状态。

LF-LSTM，aligned，5 seed：

| 变体 | MAE ↓ | Corr ↑ | Acc-2(non0) | Acc-7 |
|------|-------|--------|-------------|-------|
| 默认（按真实长度取末状态） | 0.9708 ± 0.0387 | 0.6460 ± 0.0110 | 0.7637 ± 0.0124 | 0.3464 ± 0.0262 |
| `--model-arg use_lengths=False` | 0.9541 ± 0.0268 | 0.6543 ± 0.0093 | 0.7784 ± 0.0150 | 0.3469 ± 0.0213 |

unaligned（audio/vision 有 90% 的步是 padding），20 epoch，2 seed：

| 变体 | MAE | Corr | Acc-2(non0) | Acc-7 |
|------|-----|------|-------------|-------|
| 默认 | 0.9775 ± 0.0419 | 0.6634 | 0.7889 | 0.3426 |
| `--model-arg use_lengths=False` | 0.9514 ± 0.0073 | 0.6628 | 0.7919 | 0.3389 |

- 正确性已单元验证：取第 L−1 步的输出**等于**直接在截断序列上跑 LSTM，且追加任意垃圾 padding 后输出完全不变（`scripts/check_invariants.py`）。
- **但精度上没有优势**：aligned 上掩码版反而略差（0.5σ 以内），unaligned 上同样持平。原以为 unaligned 影响会放大，实测没有——文本模态（768 维、hidden 128）主导了整个模型。
- 结论：**保留掩码为默认**，理由是语义正确、对 padding 长度不变，而不是因为它提升了指标。

## 模态消融

LF-LSTM，aligned，3 seed。这是选定研究方向的主要依据（见 `docs/roadmap.md`）：

| 模态 | MAE ↓ | Corr ↑ | Acc-2(non0) | Acc-7 |
|------|-------|--------|-------------|-------|
| 仅文本 | 0.9641 ± 0.0145 | 0.6521 ± 0.0145 | 0.7668 ± 0.0030 | 0.3421 ± 0.0131 |
| 仅音频 | 1.4469 ± 0.0137 | 0.2134 ± 0.0258 | 0.4832 ± 0.0500 | 0.1458 ± 0.0076 |
| 仅视觉 | 1.4652 ± 0.0113 | 0.0695 ± 0.0303 | 0.5030 ± 0.0081 | 0.1662 ± 0.0081 |
| 音频+视觉 | 1.4487 ± 0.0243 | 0.1469 ± 0.0170 | 0.5488 ± 0.0264 | 0.1691 ± 0.0073 |
| 三模态 | 0.9488 ± 0.0063 | 0.6484 ± 0.0008 | 0.7693 ± 0.0127 | 0.3615 ± 0.0096 |

音频/视觉单独使用时 MAE 比恒定预测 0（1.4174）还差；加到文本上只买到 0.015 MAE，落在噪声内。

## 正确性验证

`scripts/check_invariants.py`（全部通过，可随时重跑）：

| 检查 | 结果 |
|------|------|
| pkl 的 `classification_labels` == `sign(regression)+1`（0/1/2 = 负/恰好零/正） | 通过，我们不使用该字段，7 分类标签自行推导 |
| 指标实现 vs 独立手写实现（200 组随机数据，acc5/acc7 用相反的取整顺序） | 最大偏差 0.00e+00 |
| 完美/反向/常数预测、常数真值 | MAE 0、Acc2 0、corr 返回 0 而非 NaN |
| 多 seed 汇总用样本标准差（ddof=1），单次运行 std 报 0 | 通过 |
| 编码器掩码 == 截断序列；垃圾 padding 不影响输出 | 通过（差值恰好为 0） |
| `masked_mean` 只对有效帧求均值；unmasked 版按 `有效帧/填充宽度` 缩放 | 通过（比值 0.3333 == 2/6） |
| aligned 下 audio/vision 在文本长度之后全为零 | 0 处违例（故三模态可共用一个长度） |
| 评测 loader 保持数据集顺序（预测与 id 对齐） | 通过 |
| 特征 pickle 缓存的 str/Path 键归一 | 通过（否则同一个 400MB 文件缓存两份） |
| 训练结果高于平凡预测下界（全 0 / 训练均值 / 中位数，MAE 1.42-1.46） | 通过 |

`scripts/verify_runs.py`：45 个运行的指标全部能由落盘预测重算出来，预测哈希与汇总口径均一致。

## 复现性验证

| 检查 | 结果 |
|------|------|
| CUDA / CPU 同 seed 连跑两次 | 权重与预测逐比特一致 |
| 单一 trainer 重构、抽出共享池化函数、指标模块重写 | LF-LSTM 哈希始终为 `172967c7dced83b2` |
| CPU `--num-workers` 0 / 2 / 4 | 三者哈希相同 |
| CPU 线程数 1 / 4 / 12 | **三份不同结果** —— 跨机器比对必须传 `--num-threads` |
| `--num-threads N` vs `OMP_NUM_THREADS` | 前者覆盖后者 |
| MPS | **未验证**（无 Apple Silicon 硬件），请在 Mac 上先跑 `scripts/check_repro.py` |

## 待办

- 在 Apple Silicon 上确认 MPS 的确定性（`torch.gather` 反向在 MPS 上是否有确定性实现待验）
- 配对 bootstrap 显著性检验（第 1 阶段协议要求）
- 移植 MulT / MISA / Self-MM + 纯文本微调 BERT 对照组（TFN 已完成）
- 跨模型结果汇总表生成（读各 `summary.json`）
