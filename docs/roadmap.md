# 研究路线

写于 2026-07-26。方向依据见下方"动机"一节，均为本仓库实测数据，不是文献转述。

## 动机：两个实测事实

**1. MOSI 上单 seed 的波动大于文献里绝大多数"改进"。** LF-LSTM 5 个 seed（42-46，CUDA）的 test MAE 为 0.971 ± 0.039（样本标准差），最好 0.942 / 最差 1.038。而 MMSA `results/result-stat.md` 里相邻模型的差距普遍是 0.01–0.03 MAE。**该榜单相当一部分排序落在噪声内。** MOSI 测试集仅 686 条，一条样本 = 0.146% Acc-2，1% 以内的差距约等于 1σ。

**2. 在这批 2018 年的特征上，音频和视觉几乎不贡献。** LF-LSTM 模态消融（3 seed）：

| 模态 | MAE ↓ | Corr | Acc-2(non0) | Acc-7 |
|---|---|---|---|---|
| 仅文本 | 0.964±0.015 | 0.652 | 76.7% | 34.2% |
| 仅音频 | 1.447±0.014 | 0.213 | 48.3% | 14.6% |
| 仅视觉 | 1.465±0.011 | 0.070 | 50.3% | 16.6% |
| 音频+视觉 | 1.449±0.024 | 0.147 | 54.9% | 16.9% |
| 三模态 | 0.949±0.006 | 0.648 | 76.9% | 36.2% |

音频/视觉单独使用时 MAE 比恒定预测 0（1.417）还差；加到文本上只买到 0.015 MAE，落在噪声内。

限定：以上出自一个很弱的架构（a/v hidden 仅 32），MulT 的跨模态注意力、Self-MM 的自监督单模态标签能榨出更多。它标定的是信号量级，不是终局结论。

另注意 MMSA 表中 MulT(79.7) → MISA(81.8) → Self-MM(83.4) 这一跳恰好对应"开始微调 BERT"。**编码器强度的收益可能远大于融合结构设计的收益**——这是第 2 阶段要检验的核心假设。

## 与 MMSA 的关系

`/workspace/MMSA`（MIT 协议）。已核对的情况：

- `trains/singleTask/` 14 个文件、2325 行几乎重复的训练循环（`TFN.py` 与 `LMF.py` 名字归一后仅差 14 行）。加模型 = 复制 trainer，训练协议会静默漂移。
- `setup_seed()` 只设 `manual_seed` + cudnn flag，无 `use_deterministic_algorithms`、无 worker 播种——运行非逐比特可复现。
- `data_loader.__truncate` 为死代码，且内部 `do_truncate` 忽略 `length` 参数硬编码 `index:index+20`。
- `config_regression.json` 中 mosi/unaligned 的 `seq_lens` 写作 `[50,500,375]`，实际 pkl 为 audio 375 / vision 500（运行时被数据覆盖，故未致命）。
- `result-stat.md` 全为单值，无方差。

**取用策略**：模型定义可移植（署名 MIT），特征管线与结果表作为复现靶子；训练循环、评测协议、复现性基础设施由本仓库自己实现。**已验证我们的指标实现与其 `metricsTop.py` 逐位一致**（差异仅来自其 round 到 4 位小数），故两边数字可直接对比。

目标不是"做一个更大的框架"。核心保持薄——单一 trainer + 模型注册表，目标 1500 行以内（对比其 6000 行）。

## 第 1 阶段：复现（不可跳过）

移植并复现 5 个跨年代代表 + 1 个关键对照：

按技术演进排序，每一步回答"上一步缺什么"：

| # | 模型 | 解决了什么 | MMSA 报告 MAE / Acc-2(non0) |
|---|---|---|---|
| 1 | EF-LSTM / LF-DNN | 最朴素的早/晚融合，确立下限 | 0.949 / 78.5，0.955 / 78.6 |
| 2 | TFN (2017) | 用外积显式建模模态间交互 | 0.947 / 79.1 |
| 3 | LMF (2018) | 低秩分解，解决 TFN 融合层参数量爆炸 | 0.950 / 79.2 |
| 4 | MFN / Graph-MFN | 引入跨时间的记忆 | 0.927 / 78.9，0.956 / 78.4 |
| 5 | MulT (2019) | 跨模态注意力，直接处理未对齐序列 | 0.880 / 81.0 |
| 6 | MISA (2020) | 模态不变/特有表征解耦 | 0.777 / 83.5 |
| 7 | Self-MM (2021) | 自监督生成单模态标签 | 0.708 / 85.5 |
| 8 | **纯文本微调 BERT** | **对照组** | — |

**第 8 项是这条故事线真正的看点，不是补充。** 上表中 MulT(81.0) → MISA(83.5) → Self-MM(85.5) 这一跳恰好对应"开始微调 BERT"。没有纯文本对照，前七步排出来的只是一张榜单；有了它，才能判断这条故事线讲的是"融合结构在进步"还是"文本编码器在进步"——而后者正是第 2 阶段的核心假设。

### 验收标准（2026-07-26 定，不因单次结果调整）

**参照物是 MMSA 的 `results/result-stat.md`，不是原论文。** 原论文（TFN 2017、MFN 等）用的是 CMU-SDK 的 GloVe 文本特征，部分还用了不同划分；MMSA 把所有模型用统一的 BERT 特征与统一划分重跑过。拿 BERT-特征的结果去比 GloVe-特征的数字，比的是特征不是模型。原论文数字记录在案作为背景，不作为达标目标。

**超参来源**：以 MMSA 对应模型的配置为准（同协议才可比），同时查阅并记录原论文的超参。两者不一致时，差异本身就是排查线索，写进模型文件的 docstring；原论文超参无法确认时明确注明"未能确认"，不臆测。

**判据**：每个模型跑 **10 个 seed（固定 42-51，事先注册）**，取均值，用标准误（SE = σ/√10）度量差距：

| 落后 MMSA 报告值 | 判定 |
|---|---|
| ≤ 1 SE（含更优） | 复现成功 |
| 1–2 SE | 复现成功，但标记待查 |
| > 2 SE | **未复现**，进入查代码循环 |

"不能差太多"需要一个数，而我们自己 seed 的离散度是唯一诚实的标尺——MMSA 报的是单个值，没有方差。MAE 越小越好，其余越大越好。主指标为 MAE 与 Acc-2(non0)，其余一并汇报。

判定由 `scripts/check_acceptance.py` 执行，不靠人工裁量：

```bash
python scripts/check_acceptance.py tfn_mosi      # 单组
python scripts/check_acceptance.py --all         # 全部有参照的组
```

**不达标时的唯一补救手段是检查并完善代码**，直到 10 seed 的整体效果达标为止。明令禁止：

- 挑选 seed（seed 集合事先注册为 42-51，任何情况下不得为达标而更换）
- 为达标而事后调超参（复现阶段一律使用 MMSA 对应模型的超参）
- 只报好看的那个指标

若逐项核对实现后仍不达标，那是一个需要写进 `docs/experiments.md` 的发现（协议差异、或对方数字本身不稳），如实汇报，不得抹平。

**产出**：一张"哪些差距扛得住严格协议"的表。这张表本身就是产出，不只是中间步骤。

其余硬性要求：

- 与纯文本对照组做配对 bootstrap 显著性检验
- 全部运行落盘 `result.json`（超参 + 环境指纹 + git commit + 预测哈希）
- 改动 data/metrics/模型后跑 `scripts/check_invariants.py`
- 新实验进入文档时，必须同时在 `scripts/reproduce_all.sh` 里加一条

## 第 2 阶段：主课题——编码器换代 vs 融合结构

**假设：编码器变强后，融合架构之间的排序会坍缩。**

做法：用 WavLM/Whisper 换音频、VideoMAE/CLIP 换视觉，在**同一批融合架构**上重跑第 1 阶段的全部实验，控制变量只有编码器。

- 证实 → 重要的负面结果，说明社区在融合结构上的投入回报有限
- 证伪 → 说明融合设计确有独立价值，且给出它在什么编码器强度下才显现

两个方向都是真结果。基础设施完全复用第 1 阶段。

## 第 3 阶段：候选（视第 2 阶段结果再定）

- **缺失/带噪模态的鲁棒性**：真实部署问题，远未饱和，MMSA 有 `missingTask/TFR_NET` 可直接对照
- **CH-SIMS v2 的逐模态标注**：可做模态贡献归因，赛道不拥挤

## 数据集

**主数据集换成 MOSEI，MOSI 只做副。** 理由见动机第 1 条——MOSI 测试集太小，判别力不足。本地目前只有 CMU-MOSI，MOSEI 需另行下载。

## 工程基础设施

已完成：

- 设备无关（CUDA / Apple Silicon MPS / CPU），同设备同 seed 逐比特可复现
- 单一 trainer + 模型注册表；加模型不改训练循环
- 多 seed 汇总（样本标准差 ddof=1）
- 四道闸门：`check_data` / `check_invariants` / `check_repro` / `verify_runs`，`bash scripts/check_all.sh` 一次跑完
- 运行落盘 provenance（超参、环境、git commit 与 dirty 标记、预测哈希）；结果产物入库，clone 后可重新审计

后续：配对 bootstrap 显著性检验、跨模型结果汇总表生成。

## 当前进度与下一步

**本节是会话之间的交接点，每完成一件事就更新它。** 最后更新：2026-08-01。

### 第 1 阶段（复现）进度：14 个模型全部通过

复现范围两次扩张：2026-07-27 定为 11 个（MMSA 的 MOSI 表能提供参照值的全部范围，止于 CENET/TETFN）；07-31 加入 BERT-MAG / MMIM / ALMT 三个——它的表没登记，参照改用**我们自己跑它的代码**，反而是更强的参照（带方差）。

**逐模型判据（`check_acceptance.py --all`）：14 组全部通过，退出码 0。** 全部数字与逐节解读见 `docs/storyline.md` 的进度总表。其中 TFN 与 EF-LSTM 的 Corr 缺口经跑 MMSA 自己的代码裁定（`acceptance_status.json`），MulT 的 Corr 是勉强通过（0.0105 对地板 0.0110）。

### 整体打平的问题：已回答（2026-07-31）

逐模型全过**不等于**整体打平——逐模型判据看不见跨模型的系统性偏置（`#systematic-bias`，2026-07-27 起挂着）。现在有了跨模型聚合判据（`aggregate_acceptance.py`，判据**先立后测**），参照也补到了与我们同精度（14 模型 × 10 seed，140 次运行）：

| 被检验方 | 参照 | MAE Z | Corr Z | 判定 |
|---|---|---|---|---|
| **我们** | **MMSA 的代码** | **−2.21**（p=0.027） | **−3.17**（p=0.0015） | **超过** |
| 我们 | MMSA 公开表 | +7.52 | +6.96 | 落后 |
| MMSA 的代码 | MMSA 公开表 | +8.54 | +11.28 | 落后**更多** |

**对能复现的基准我们超过它**；对那张表两份实现都落后，而我们更接近。稳健的符号检验是 3/14、p=0.057，故严格表述为"至少打平，方向一致偏向我们"。限定见 [`investigations.md#aggregate-verdict`](investigations.md#aggregate-verdict)。

> **⚠️ 上表是旧机器（RTX 5070 Ti）的结果，已被 2026-08-02 的重跑推翻。** 两侧都在 RTX 5080 上重跑后：**MAE Z=+0.17（p=0.864）、Corr Z=+1.88（p=0.060），判定为「打平」而非「超过」。** 成因是 EF-LSTM 一个模型（Corr z=+14.86，来自崩溃计数 2/10 对 0/10 的抽样运气）；排除它后仍为"超过"（Corr p=0.030）。**代码与判据一字未改，只换了 GPU。** 完整记录见 [`investigations.md#rebaseline-5080`](investigations.md#rebaseline-5080)。

这一轮顺带做完/查清的：

- 参照逐 seed 入库（`docs/mmsa_code_runs_mosi.json`）+ 生成脚本入库；修掉参照 sd 的 ddof=0/1 混用（判定未变）
- 撤回"改用配对检验"的计划——实测同 seed 跨实现相关性 −0.42…+0.32，**配对前提不成立**
- 验证选择口径的量级：TFN 上值 0.016 MAE（配对 t=−2.53），MFN 上 9/10 seed 无变化。机制是末批超权，**方向不一致，不是系统性偏置的解释**，不采用
- MMSA 表写于 2021-05-06，其后 8/11 模型超参被改过。**TFN 用 2021 配置更差（假说证伪）**；**EF-LSTM 用 2021 配置零崩溃、缺口从 0.1229 缩到 0.0170 MAE**——崩溃是四层堆叠引入的
- MFN 对原作者实现核对完毕：无发现

### 第 1 阶段收尾（2026-08-01）

上一版列在「下一步」里的第 1、4 两项已完成，第 1 阶段到此可以认为收口：

- **聚合判据已纳入闸门**（`2dac00c`）。`check_all.sh` 现在同时跑逐模型与聚合两套判据，任一回归都会亮红。参照取 MMSA 的**代码**而非其公开表——表是被检验的主张，而本仓库的任何改动都够不到它（MMSA 自己的实现差得更多），挂在那里只会是一盏不携带信息的长红灯。
- **bs 16 的可检验预测已验证**（`24c8004`）：**方向成立，精度有限**。四个点按末批超权倍数排序单调（1.13×→1/10, 3.05×→2/10 与 4/10, 5.73×→8/10），两个 bs 16 模型都落在锚点之间。但同样 3.05× 的两个模型相差一倍，且 n=4 做不了显著性声明。完整限定见 [`investigations.md#select-reduction`](investigations.md#select-reduction)。
  - **比结论更值得记的**：检验量（改变了所选 epoch 的 seed 比例）是在看数据之前定死的。若改用 |ΔMAE|，同一批运行会读成"预测失败"——它在这里并不单调。

### 换机器（2026-08-01）

从原机器（RTX 5070 Ti）迁到新实例（RTX 5080 + Ryzen 7 7700）。环境已重建、数据集 sha256 校验通过、全部闸门绿。**换机器带来两个必须知道的后果**：

1. **约定 6 的锚点哈希 `172967c7dced83b2` 在本机失效**，且无法靠任何代码改动恢复。已用 2×2 交叉实验排除代码原因——同机器上 `8ea438f1` 与 HEAD 在 CUDA/CPU 上均逐比特相同，约定 6 成立。方法与数据见 [`investigations.md#cross-machine-hash`](investigations.md#cross-machine-hash)。
2. **`docs/experiments.md` 的数字在本机不会逐位重现**，`reproduce_all.sh` 会红。差异不是末位几个 bit：early stopping 把微小数值差放大成不同轨迹（LF-LSTM seed 42 的 best_epoch 13→23、MAE 0.9543→0.9318，仍在该模型 seed 间标准差 0.039 之内）。

顺带查清：**CPU 也不跨机器复现**，即使钉住线程数、库版本完全相同——`migration.md` 与 `README.md` 原先的说法已订正。

### 参照环境已重建（2026-08-01）

上一版「下一步」的第 1 项已完成。`/workspace/MMSA` 与 `/workspace/mmsa_env` 都回来了，重建步骤固化为 **`bash scripts/setup_mmsa_reference.sh`**（幂等；以等价检查收尾，且**不看退出码看结论**，因为 SKIP 时退出码也是 0）。

- **`check_almt_equivalence.py` 现在真的在比对**：183 个参数张量复制后两侧输出 max abs diff `0.000e+00`。约定 5 那道网接回来了。
- 它失效有**两个**原因，此前只知道一个：除 MMSA 检出不在之外，脚本里的 shim 路径硬编码成某次会话的临时目录，**那个路径在旧机器上也活不过会话结束**——这道闸门不是从换机器才开始静默失效的。现读 `MMSA_SHIM`，默认 `/workspace/mmsa_env/shim`。
- **顺带测出参照本身也绑定机器**：MMSA 自己的代码跑 tfn seed 42，旧机 MAE 0.9305 / Corr 0.6789，本机 0.9441 / 0.6576，量级与我们那次（0.0225 MAE）相同。含义写进了下面的待决项。

### 环境缺口已补（2026-08-01）

主 venv 里原本**没有 `transformers`**，8 个 BERT 系模型（misa / self_mm / cenet / tetfn / bert_mag / mmim / almt / text_bert）在本机一个也训不了，而 `reproduce_all.sh` 里有 34 处引用它们。`pyproject.toml` 与 `requirements-lock.txt` 都漏登记了这个依赖，所以 `setup.sh` 在**任何**新机器上装出来的都是个跑不了一半模型的环境，且**没有任何闸门会红**（闸门只读已存结果，从不构造模型）。

已装 5.14.1 并钉进 lock，`collect_env()` 开始记录它的版本。**验证方式是八个模型各用自己的真实配置训一个 epoch，全部通过**，不是"能 import"。选版理由与已否决项见 [`decisions.md`](decisions.md)。

### 重立基线已完成（2026-08-02）

两侧都在本机重跑：我们 38 组（5 小时），MMSA 代码参照 16 模型 × 10 seed（4.5 小时，含新增的 mctn / mfm），**均无失败**。参照文件现按模型记 `machine` 字段，`collect` 会拒绝跨机器合并。三个结论：**MFN 判定翻面**、**聚合判定 AHEAD → LEVEL**、**锚点哈希已在本机重立**（`199f629bfb036c93` / `969d5a4579736f8f`）。见 [`investigations.md#rebaseline-5080`](investigations.md#rebaseline-5080)。

### 下一步（按顺序）

1. **把 `docs/experiments.md` 正文迁到本机数字**。`outputs/` 已全部是本机结果，正文仍是旧机器的，文档顶部已挂迁移横幅。跨模型总表可用 `scripts/summary_table.py` 现生成；`storyline.md` 的进度总表同理。
2. **MCTN + MFM 收口**：参照已就位（各 10 seed，本机）。原作者实现均已核实可获取（[hainow/MCTN](https://github.com/hainow/MCTN)、[pliang279/factorized](https://github.com/pliang279/factorized/)），可按约定 5 双向核对。做完第 1 阶段完整。
3. **预注册稳健性判据**：崩溃运行如何计入。**本轮不得追溯适用**（已看到结果），在 MOSEI 或第 2 阶段首次应用时同时报新旧两套判据。
4. **MOSEI**：主数据集。换数据集后 `NOISE_FLOOR` 必须重测，SE 封顶与地板都依赖它。**下载前先量磁盘余量。**
5. **第 2 阶段**：编码器换代 vs 融合结构（见上）。第 1 阶段的全部基础设施可直接复用。

新方法的调研台账见 [`survey.md`](survey.md)：近三年 11 篇已核实，8 篇有可运行官方代码，DPDF-LQ（EMNLP 2025，带权重 + MIT）优先。

### 阻塞项与待决

- **待人拍板：本机要不要重立数值基线？** 涉及两件事——约定 6 的锚点哈希在本机重新生成（并标注绑定机器），以及 `reproduce_all.sh` 重跑一遍把 `docs/experiments.md` 换成本机数字。**代价是历史数字与新数字混在一起**，且旧机器已不可用、无法回头验证。另一条路是维持现状、承认那些数字属于旧机器，只在本机新增结果时另立一套。**这个决定应当先记进 `decisions.md` 再动手，不要默认执行。**
  - 2026-08-01 补：**参照侧（`docs/mmsa_code_runs_mosi.json`）与我们侧处境完全相同**，都是旧机器的数字。所以聚合判据现在是同机器配对、自洽的；**只重跑一侧会把两台机器混进同一个分布，比两侧都不重跑更糟**。要重立就两侧一起重立。
- **已入库结果的 `transformers` 版本与 CPU 型号永久不可考。** `env` 从 2026-08-01 起才记 `transformers` / `cpu` / `cpu_capability` 三个字段，此前只有 python / torch / numpy。已入库的 8 个 BERT 模型结果只能确定是 5.x（代码里"transformers >= 5 返回张量、旧版返回元组"的兼容分支）；旧机器的 CPU 型号与向量化分派档位则完全没有记录，`investigations.md#cross-machine-hash` 的 ISA 假设因此**永远不会被验证**。字段本身已补上，救的是下一次换机器。
- **MOSEI 尚未下载**。路线图定的是主数据集用 MOSEI（MOSI 测试集仅 686 条，判别力不足）。
- **`/workspace` 不是持久卷**（本实例 `workspace_is_volume: false`）。recycle 或 destroy 会抹掉整个目录，只有推到远端的东西存活。数据集（879MB）和 venv 届时都要重来。
  - **这条已经兑现过一次**：此前有一台实例磁盘写满、SSH 登不上去，未推送的工作全部报废。所以纪律是**做完一件推一件**，不要攒到阶段末尾。`check_all.sh` 现在每次都会报磁盘余量与未推送量（低于 3G 转红）——是警告不是闸门，因为闸门红了 `sync.sh` 就不推，而磁盘紧张正是最该推的时候。
  - 当前余量 **6.4G / 16G**（`.venv` 独占 7.2G，刚清掉 1.2G 的 pip 缓存）。**下载 MOSEI 前必须先量余量**——它比 MOSI 的 879MB 大得多，是下一个最可能把盘写满的东西。
- **MPS 未在真机验证**。无 Apple Silicon 硬件。
- EF-LSTM 的崩溃 seed 说明判据缺稳健性维度，见 `docs/investigations.md#ef-lstm-collapse`。改判据前先记决策，**不得在已有结果之后调整以迎合结果**。
